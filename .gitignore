# tick ledger (managed by the tick CLI on an orphan branch)
/.tick
.tick.lock

# Node / Vite
node_modules/
dist/
coverage/
*.local
.vite/

# Editor / OS
.DS_Store
*.log

# Local, uncommitted real-data corpus (may carry identity-correlating AIDs)
/samples/

# Phase-1 UX working material — quotes specifics from the real (identity-correlating) corpus
/docs/ux/phase1-strategy.md

# Design bake-off prototypes — embed real corpus data (PII/AIDs), local-only
/design/

# Adversarial critique output — may quote the real (PII-bearing) corpus
/reviews/

# Engine spike prototype (informs the TDD engine; not product code)
/spike/
