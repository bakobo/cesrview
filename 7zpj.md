# Prepare a contribution of the in-browser TS CESR walker (src/cesr) to signify-ts (h6rk4d: upstream the native-TS stream-walker)
kind: todo
created: 2026-07-21T16:01Z

- 2026-07-21T19:10Z v1 parser PREPARED on ~/code/wot/signify-ts branch feat/cesr-stream-parser (commit 5072a6b): src/keri/core/parsing.ts + export + test/core/parsing.test.ts (30 inline keripy-vector tests). All 4 CI gates green (build, lint, pretty:check, own tests). Based on upstream/main for a clean WebOfTrust PR. Remaining: maintainer opens PR (push to provenant-dev fork). v2 machinery = separate follow-up (see this.i p2rz8k / h6rk4d).
- 2026-07-21T20:53Z PR RAISED: https://github.com/WebOfTrust/signify-ts/pull/402 — from personal fork dhh1128/signify-ts (NOT provenant), base WebOfTrust:main, author dhh1128, mergeable. Adds src/keri/core/parsing.ts (side-effect-free, zero new deps) + export + 30 inline keripy-vector tests; all 4 CI gates green locally. Awaiting 2-of-3 maintainer review. v2 machinery = separate follow-up (p2rz8k / h6rk4d).
- 2026-07-22T03:59Z v2 machinery PR RAISED (stacked on #402): https://github.com/WebOfTrust/signify-ts/pull/403 from dhh1128 branch feat/cesr-stream-parser-v2 (commit 3ecbb53), base WebOfTrust:main, MERGEABLE. Ports cesrview's native v2 framing into parsing.ts: v2 version-string parse + genus dispatch + native v2 counter tables (keripy CtrDex_2_0; v2 groups self-frame as count*4) + genus on AttachmentGroup; primitives still via Matter/Indexer. +342 lines incl. v2 tests + a real keripy-2.0.0-dev6 OOBI vector; all 4 CI gates green locally. Shows both commits until #402 merges, then narrows to the v2 commit. Executes the p2rz8k follow-up.
- 2026-09-03T15:57Z 2026-09-03 daidoji requested changes on #402: a stream whose version string declares more bytes than exist parsed as a COMPLETE message, no error, consumed/span a megabyte past the buffer. Confirmed, plus two more of the same root cause — a truncated -V framed 'known' past the buffer, and a size-0 version string with an undecodable kind looped forever (25 bytes -> 4 GB heap death in ~40s; the browser tab, here). Fixed in @jr9p4w/@z3fn5v: walk.ts commit a007a6c (cesrview), and ported to signify-ts branches feat/cesr-stream-parser (ff79077, the #402 fix) and feat/cesr-stream-parser-v2 (ee410e3, rebased on it, carrying the v2 bounds checks). NOT PUSHED — awaiting Daniel. Reply drafts at /tmp/pr402-reply-daidoji.md and /tmp/pr403-reply-daidoji.md; his #403 report does NOT reproduce (the CESR-spec SAD Path ACDC parses cleanly, and his assert.equal(errors, []) throws unconditionally in chai).
- 2026-09-03T16:46Z 2026-09-03 (later) CI's 100% coverage gate failed on a007a6c — npm test passes but npm run coverage is the gate, and the bounds checks split one-line failure paths in two, leaving six branches untested. Covering them exposed a real defect: signify-ts's Matter/Indexer Sizes store a variable-size code's fs as null while typing it 'fs?: number', so 'fs === undefined || fs < 0' let null through (null >= 0 is true). Fixed by value with typeof in cesrview 1098cdc and signify-ts 43e1be6. All three branches now pushed: cesrview main 1098cdc (CI green), #402 head 43e1be6, #403 head 6caf3be. LESSON: run npm run coverage in cesrview, not npm test.
